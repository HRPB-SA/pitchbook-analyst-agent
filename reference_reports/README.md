# Reference reports

Drop any externally produced .docx reports here; `python3 -m engine style`
folds them into the measured style profile alongside the shipped library in
reports/*/output and output/.
